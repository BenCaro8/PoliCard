import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    display: 'flex',
    flexDirection: 'row',
  },
  star: {
    marginHorizontal: 1,
    marginTop: 2,
  },
  numVotes: {
    color: 'white',
    marginRight: 5,
  },
});

export default styles;
